<Window x:Class="WpfScheduledApp20250729.Views.UpdateTaskWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfScheduledApp20250729.Views"
        mc:Ignorable="d"
        Title="{Binding WindowTitle}" Height="600" Width="500"
        WindowStartupLocation="CenterScreen">
    
    <Window.Resources>
        <Style TargetType="Label">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,5,0,2"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Height" Value="25"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding HasError}" Value="True">
                    <Setter Property="BorderBrush" Value="Red"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="DatePicker">
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="Height" Value="25"/>
        </Style>
        <Style x:Key="ErrorTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="Red"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Margin" Value="0,-8,0,8"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Form Fields -->
        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding FormFields}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="0,0,0,5">
                            <Label Content="{Binding DisplayName}"/>
                            
                            <!-- Text Field -->
                            <TextBox Visibility="{Binding FieldType, Converter={x:Static local:FieldTypeToVisibilityConverter.Instance}, ConverterParameter=Text}"
                                     Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}"/>
                            
                            <!-- TextArea Field -->
                            <TextBox Visibility="{Binding FieldType, Converter={x:Static local:FieldTypeToVisibilityConverter.Instance}, ConverterParameter=TextArea}"
                                     Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}"
                                     Height="80" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>
                            
                            <!-- Number Field -->
                            <TextBox Visibility="{Binding FieldType, Converter={x:Static local:FieldTypeToVisibilityConverter.Instance}, ConverterParameter=Number}"
                                     Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}"/>
                            
                            <!-- Date Field -->
                            <DatePicker Visibility="{Binding FieldType, Converter={x:Static local:FieldTypeToVisibilityConverter.Instance}, ConverterParameter=Date}"
                                        SelectedDate="{Binding Value, UpdateSourceTrigger=PropertyChanged}"/>
                            
                            <!-- Time Field -->
                            <TextBox Visibility="{Binding FieldType, Converter={x:Static local:FieldTypeToVisibilityConverter.Instance}, ConverterParameter=Time}"
                                     Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}"
                                     ToolTip="Enter time in HH:MM format (24-hour)"/>
                            
                            <!-- Boolean Field -->
                            <CheckBox Visibility="{Binding FieldType, Converter={x:Static local:FieldTypeToVisibilityConverter.Instance}, ConverterParameter=Boolean}"
                                      IsChecked="{Binding Value, UpdateSourceTrigger=PropertyChanged}"
                                      Content="{Binding DisplayName}"/>
                            
                            <!-- Error Message -->
                            <TextBlock Text="{Binding ErrorMessage}" 
                                       Style="{StaticResource ErrorTextStyle}"
                                       Visibility="{Binding HasError, Converter={x:Static local:BooleanToVisibilityConverter.Instance}}"/>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Buttons -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Cancel" Command="{Binding CancelCommand}" 
                    Width="80" Height="35" Margin="0,0,10,0"/>
            <Button Content="Send" Command="{Binding SendCommand}" 
                    Width="80" Height="35" IsDefault="True"
                    Background="DodgerBlue" Foreground="White"/>
        </StackPanel>
    </Grid>
</Window>